#include <stdio.h>

int main()
{
  for (int i = 1; i <= 5; i++)
  { // Se inicializa i en 1, el ciclo sigue mientras i <= 5, se incrementa i en cada iteración
    printf("Número: %d\n", i);
  }

  return 0;
}
