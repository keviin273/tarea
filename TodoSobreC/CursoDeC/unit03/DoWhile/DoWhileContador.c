#include <stdio.h>

int main()
{
  int i = 1;

  do
  {
    printf("Número: %d\n", i);
    i++; // Incrementamos la variable i
  } while (i <= 5); // Repite hasta que i sea mayor que 5

  return 0;
}
